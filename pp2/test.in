int a;
double b;
bool c;
string d;
name_type e;
name_type[]
[] f;

void dede(int a, double b) {
	a = b;
}

int ded(int a, double b) {
	a = b;
	14;
	0.912;
	"abcd";
	false;
	null;
	a[123];
	a[d];
	b.d;
	b.
	a[
	1];
	this;
	asdb.adsbd(a, b, p[7], acd(9));
	(asdb.adsbd(a, b, p[7],
	acd(9)));
	bb  + u78;
	bb  - u78;
	bb  * u78;
	bb  / u78;
	bb  % u78;
	-p(7, u[7]);
	- -a - -7;
	-a * h;
	-h % -a;
	-a-b;
	-a < -b;
	a > b;
	a >= b;
	a <= b;
	a == b;
	a != b;
	a -b > c;
	-
	c;
	a && b || c && d && !w;
	-!a;
	b * -!-a;
	ReadInteger
	(
	);
	ReadLine(
	);
	New (
	abc
	)
	;
	New (Double);
	New (
	Bool
	);
	NewArray (3 + k[9].pa(12), Double);;;;;
	;
	if (l) if (a) {c;} else {d;} else abd(9);
	while (8)
	{
		if (l) k;
		a;
	}
	for (a = 0; a < b; a = a + 1)
	{
		d + e;
		c + f;
	}
	for (; a < b; a = a + 1)
	{
		d + e;
		c + f;
	}
	for (a = 0; a < b;)
	{
		d + e;
		c + f;
	}
	for (; a < b;)
	{
		d + e;
		c + f;
	}
	break;
	return ;
	return p;
	Print (a, b, c, e[6], f.o(a));
	{{{
		a;
		intab;
		c;
	}}}
}

class aaaaa extends bbbb implements a, b, c {
	int a;
	void make(int a, double b) {
		int a;
		a + b;
	}
}

class aaaaa implements a, b, c {
	int a;
	void make(int a, double b) {
		int a;
		a + b;
	}
}

class aaaaa extends bbbb {
	int a;
	void make(int a, double b) {
		int a;
		a + b;
	}
}

class aaaaa  {
	int a;
	void make(int a, double b) {
		int a;
		a + b;
	}
}

interface inter {
	void aaaa(int a, double d, bool c);
}